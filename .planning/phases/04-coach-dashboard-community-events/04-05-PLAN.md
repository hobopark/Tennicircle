---
phase: 04-coach-dashboard-community-events
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - EVNT-04
  - EVNT-05
  - EVNT-06

must_haves:
  truths:
    - "Database schema is pushed to Supabase and tables exist"
    - "All pages render without errors in browser"
    - "Events can be created, RSVP'd to, and viewed"
    - "Coach dashboard day/week toggle works"
    - "Bottom navigation works for all roles"
  artifacts: []
  key_links: []
---

<objective>
Push database schema to Supabase and verify the full phase end-to-end.

Purpose: [BLOCKING] Schema must be pushed before any runtime testing is possible. After push, verify all features work together. This is the integration gate.
Output: Live database tables, verified working features, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-coach-dashboard-community-events/04-CONTEXT.md
@.planning/phases/04-coach-dashboard-community-events/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push database schema to Supabase</name>
  <files></files>
  <read_first>
    supabase/migrations/00005_events_schema.sql
  </read_first>
  <action>
Run the Supabase schema push to apply migration 00005_events_schema.sql:

```bash
supabase db push
```

If this requires interactive prompts (e.g., confirming destructive changes), and `SUPABASE_ACCESS_TOKEN` env var is set, it should work non-interactively. If it prompts interactively, flag for manual intervention.

After push succeeds, verify tables exist:
```bash
supabase db dump --schema public | grep -E "CREATE TABLE.*events|CREATE TABLE.*event_rsvps|CREATE TABLE.*announcements"
```

Run a final build to confirm everything still compiles:
```bash
npx next build
```
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits with success (exit code 0)
    - `npx next build` succeeds after schema push
    - events, event_rsvps, and announcements tables exist in live database
  </acceptance_criteria>
  <done>Database schema pushed to Supabase, all 3 new tables and storage bucket live</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of Phase 4 features</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for approval. Start the dev server with `npm run dev` if not already running. Guide the user through testing each feature area.
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx next build 2>&1 | tail -3</automated>
  </verify>
  <done>User has verified all Phase 4 features work correctly and typed "approved"</done>
  <what-built>
Complete Phase 4: Coach Dashboard and Community Events system including:
- Coach schedule with day/week toggle and attendee previews (DASH-01, DASH-02)
- Player roster with attendance dates (DASH-03)
- Events system: create events (tournament/social/open session), RSVP, announcements (EVNT-01 through EVNT-06)
- Client dashboard with greeting, stats, upcoming sessions/events (D-11, D-12, D-13)
- Bottom tab navigation for all roles (D-07)
  </what-built>
  <how-to-verify>
1. **Coach Dashboard (DASH-01, DASH-02):**
   - Sign in as a coach
   - Navigate to Schedule tab
   - Verify day/week toggle appears and switches views
   - In day view: verify session cards show time, title, venue, attendee avatars
   - Tap a session card to verify it navigates to session detail
   - Switch back to week view and verify it still works
   - Refresh the page — verify the toggle remembers your last selection

2. **Player Roster (DASH-03):**
   - Navigate to Clients tab
   - Verify player rows show "Last session:" and "First lesson:" dates
   - If a player has no sessions, verify "No sessions yet" is shown

3. **Events Page (EVNT-01 through EVNT-06):**
   - Navigate to Events tab (should be visible in bottom nav)
   - Verify Official and Community tabs exist, Official is default
   - Tap "Create Event" (FAB on mobile, button on desktop)
   - Create a tournament event — verify type selector shows 3 options
   - Create a social event with a capacity
   - Verify events appear on correct tab (coach/admin events on Official, member events on Community)
   - RSVP to an event — verify "You're going!" toast
   - Tap an event card to view detail page
   - Verify event detail shows RSVP card, description, attendee list

4. **Announcements (EVNT-05):**
   - As coach, tap "Post announcement" on Official tab
   - Post an announcement — verify it appears as pinned card at top of Official tab
   - Sign in as a client — verify the announcement is visible

5. **Client Dashboard (D-11, D-12, D-13):**
   - Sign in as a client
   - Verify landing page shows "G'day, {name}" greeting
   - Verify stats strip: sessions this month, upcoming RSVPs, member since
   - Verify upcoming sessions and upcoming events sections show cards
   - Verify announcements section shows recent announcements (if any)

6. **Bottom Navigation (D-07):**
   - Verify bottom tab bar appears on all pages
   - Verify correct tabs for each role (admin sees Admin/Schedule/Sessions/Clients/Events/Profile; coach sees Schedule/Clients/Events/Profile; client sees Sessions/Events/Profile)
   - Verify active tab is highlighted with green
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema push -> live DB | Migration modifies production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | schema push | accept | Migration is version-controlled and reviewed; Supabase applies migrations sequentially |
</threat_model>

<verification>
- Schema push succeeds
- All pages load without errors
- Human verifies full flow works end-to-end
</verification>

<success_criteria>
- Database schema live with events, event_rsvps, announcements tables
- Human approval of all Phase 4 features
</success_criteria>

<output>
After completion, create `.planning/phases/04-coach-dashboard-community-events/04-05-SUMMARY.md`
</output>
