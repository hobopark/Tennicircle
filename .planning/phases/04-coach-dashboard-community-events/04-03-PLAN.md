---
phase: 04-coach-dashboard-community-events
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/calendar/WeekCalendarGrid.tsx
  - src/app/coach/page.tsx
  - src/app/coach/clients/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Coach can toggle between day and week view on the schedule page"
    - "Day view shows session cards with time, title, venue, attendee count, and attendee avatar strip"
    - "View preference persists in localStorage across page loads"
    - "Player roster shows last attended date and first lesson date per player"
    - "Session cards show inline attendance preview (confirmed/capacity and first few attendee avatars)"
  artifacts:
    - path: "src/components/calendar/WeekCalendarGrid.tsx"
      provides: "Extended calendar with day/week toggle and day view"
      contains: "DayView"
    - path: "src/app/coach/page.tsx"
      provides: "Coach schedule page with day/week toggle in header"
      contains: "Schedule"
    - path: "src/app/coach/clients/page.tsx"
      provides: "Player roster with attendance data"
      contains: "Last session"
  key_links:
    - from: "src/app/coach/page.tsx"
      to: "src/components/calendar/WeekCalendarGrid.tsx"
      via: "view prop for day/week toggle"
      pattern: "view.*day.*week"
    - from: "src/app/coach/page.tsx"
      to: "src/components/calendar/WeekCalendarGrid.tsx"
      via: "attendeePreview prop containing confirmed count and member avatars per session"
      pattern: "attendeePreview|confirmedCount"
    - from: "src/app/coach/clients/page.tsx"
      to: "session_rsvps"
      via: "Supabase query for first/last attendance"
      pattern: "session_rsvps"
---

<objective>
Enhance the coach dashboard with day/week schedule toggle and improve the player roster with attendance data.

Purpose: DASH-01 through DASH-03 — coaches can see their schedule in daily detail with attendee previews, and view player attendance patterns. These are the core coach productivity features.
Output: Enhanced WeekCalendarGrid with day view, updated coach page with toggle, enhanced roster page with attendance dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-coach-dashboard-community-events/04-CONTEXT.md
@.planning/phases/04-coach-dashboard-community-events/04-RESEARCH.md
@.planning/phases/04-coach-dashboard-community-events/04-UI-SPEC.md
@.planning/phases/04-coach-dashboard-community-events/04-01-SUMMARY.md

<interfaces>
<!-- Existing components being extended -->

From src/components/calendar/WeekCalendarGrid.tsx (existing — must read full file before modifying):
- Currently renders a week grid with session data
- Must ADD day/week toggle, not rewrite (per D-01)

From src/app/coach/page.tsx (existing — must read full file):
- Server Component that fetches sessions and renders WeekCalendarGrid
- Two-query merge strategy for coach sessions (template-owned + co-coached)

From src/app/coach/clients/page.tsx (existing — must read full file):
- Server Component listing assigned players
- Currently shows player name and avatar

From src/lib/types/auth.ts:
```typescript
export type UserRole = 'admin' | 'coach' | 'client' | 'pending'
export interface JWTCustomClaims { community_id: string; user_role: UserRole }
```

From src/components/profile/InitialsAvatar.tsx:
- Existing avatar component for attendee strips
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coach schedule day/week toggle with day view and inline attendance preview</name>
  <files>src/components/calendar/WeekCalendarGrid.tsx, src/app/coach/page.tsx</files>
  <read_first>
    src/components/calendar/WeekCalendarGrid.tsx
    src/app/coach/page.tsx
    src/components/profile/InitialsAvatar.tsx
    src/lib/types/sessions.ts
    .planning/phases/04-coach-dashboard-community-events/04-UI-SPEC.md
  </read_first>
  <action>
**Extend `src/app/coach/page.tsx`** (Server Component):

1. Update the data-fetching query to include attendee data for each session. For each session, fetch the first 5 confirmed RSVPs with member display_name and avatar_url using a joined query:
```typescript
// After fetching sessions, fetch attendees for all session IDs in one query
const sessionIds = allSessions.map(s => s.id)
const { data: attendees } = await supabase
  .from('session_rsvps')
  .select('session_id, rsvp_type, member:community_members(display_name, avatar_url)')
  .in('session_id', sessionIds)
  .is('cancelled_at', null)
  .order('created_at', { ascending: true })
```
2. Group attendees by session_id in JS. For each session, compute `confirmedCount`, `capacity`, and `attendeePreview` (first 5 confirmed members with display_name/avatar_url).
3. Pass this enriched data to WeekCalendarGrid as an `attendeeData` prop — a `Record<string, { confirmedCount: number, capacity: number | null, attendeePreview: Array<{ display_name: string | null, avatar_url: string | null }> }>` keyed by session ID.
4. Update page header to match UI-SPEC: `flex items-center justify-between mb-4`
   - Left: `font-heading font-bold text-2xl text-foreground` — "Schedule"
   - Right: day/week toggle pill + existing "Create session" button in a row

**Extend `src/components/calendar/WeekCalendarGrid.tsx`** (Client Component — already has `'use client'`):

1. **Do NOT rewrite** — add to the existing component (per D-01).
2. Add an `attendeeData` prop to receive enriched attendee information from the server page.
3. Add a `view` prop: `view: 'day' | 'week'` — controlled from parent or internal state.
4. Add internal state for view toggle if not controlled:
```typescript
const [view, setView] = useState<'day' | 'week'>('week')
useEffect(() => {
  const saved = localStorage.getItem('tennis-schedule-view')
  if (saved === 'day' || saved === 'week') setView(saved)
}, [])
const handleViewChange = (v: 'day' | 'week') => {
  setView(v)
  localStorage.setItem('tennis-schedule-view', v)
}
```
Note: Default to 'week' to avoid SSR hydration mismatch; useEffect reads localStorage after mount.

5. **Day/Week toggle pill** (render at top of component or accept as external):
   - Container: `bg-muted rounded-full p-1 flex gap-1 h-8`
   - Each segment: `px-3 text-sm rounded-full h-6 flex items-center transition-colors cursor-pointer`
   - Active: `bg-accent text-accent-foreground font-heading font-bold`
   - Inactive: `text-muted-foreground`
   - Labels: "Day" | "Week"
   - On click: `handleViewChange('day')` / `handleViewChange('week')`

6. **When view === 'week':** Render existing grid unchanged. Add attendee count badges to existing session slots showing `{confirmedCount}/{capacity}` from attendeeData in `text-[10px] text-muted-foreground`.

7. **When view === 'day':** Render new DayView section:
   - Date navigation row: `flex items-center justify-between mb-4`
     - Left chevron: `w-8 h-8 rounded-xl bg-muted flex items-center justify-center` with Lucide `ChevronLeft`, `aria-label="Previous day"`
     - Center: `font-heading font-bold text-base` — formatted as "Tue, 8 Apr"
     - Right chevron: same with `ChevronRight`, `aria-label="Next day"`
   - Internal state `selectedDate` (default: today)
   - Filter sessions to selected date
   - Session list: `flex flex-col gap-3`
   - Each session card (per D-03 and UI-SPEC):
     ```
     bg-card rounded-3xl border border-border/50 p-4 active:scale-[0.98] transition-transform cursor-pointer
     ```
     - Top row: Time (`text-sm font-bold text-foreground` e.g. "6:00 PM") right-aligned capacity (`text-[10px] text-muted-foreground` e.g. "4/8") — use confirmedCount/capacity from attendeeData
     - Title: `font-heading font-bold text-base`
     - Venue: `text-sm text-muted-foreground` with Lucide `MapPin` `w-3 h-3`
     - Attendee avatar strip: `flex -space-x-1`, max 5 shown — use attendeePreview from attendeeData
       - Each: `w-6 h-6 rounded-full ring-2 ring-background` — use InitialsAvatar or `<img>` for avatar_url
       - Overflow pill: `w-6 h-6 rounded-full bg-muted flex items-center justify-center text-[10px] text-muted-foreground` — "+{N}"
     - Card wraps in `<Link href={/coach/sessions/${session.id}}>` or existing session detail navigation
   - Empty state: Lucide `CalendarX` `w-8 h-8 text-muted mb-3` centered + `text-sm text-muted-foreground` — "No sessions scheduled for this day."

8. **Framer Motion animations for day view cards:**
   - Wrap each card in `<motion.div>` with `initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.05 }}`
   - Import `motion` from `framer-motion`
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/calendar/WeekCalendarGrid.tsx contains `tennis-schedule-view` (localStorage key)
    - src/components/calendar/WeekCalendarGrid.tsx contains `Day` and `Week` (toggle labels)
    - src/components/calendar/WeekCalendarGrid.tsx contains `bg-accent text-accent-foreground` (active toggle pill)
    - src/components/calendar/WeekCalendarGrid.tsx contains `ChevronLeft` and `ChevronRight` (date navigation)
    - src/components/calendar/WeekCalendarGrid.tsx contains `CalendarX` (empty state icon)
    - src/components/calendar/WeekCalendarGrid.tsx contains `No sessions scheduled for this day`
    - src/components/calendar/WeekCalendarGrid.tsx contains `motion.div` or `motion` import (framer-motion)
    - src/components/calendar/WeekCalendarGrid.tsx contains `-space-x-1` (attendee strip)
    - src/components/calendar/WeekCalendarGrid.tsx contains `attendeeData` or `attendeePreview` (prop for enriched attendee data)
    - src/app/coach/page.tsx contains `Schedule` heading text
    - src/app/coach/page.tsx contains `session_rsvps` (attendee query)
    - src/app/coach/page.tsx contains `confirmedCount` or `attendeePreview` (enriched data computation)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Coach schedule has day/week toggle with localStorage persistence, day view shows session cards with attendee avatars, week view gains attendance counts, framer-motion stagger animations on day view cards, attendee data flows from server page through attendeeData prop into grid component</done>
</task>

<task type="auto">
  <name>Task 2: Player roster with attendance dates</name>
  <files>src/app/coach/clients/page.tsx</files>
  <read_first>
    src/app/coach/clients/page.tsx
    src/lib/types/profiles.ts
    src/components/profile/InitialsAvatar.tsx
    .planning/phases/04-coach-dashboard-community-events/04-UI-SPEC.md
    .planning/phases/04-coach-dashboard-community-events/04-RESEARCH.md
  </read_first>
  <action>
**Enhance `src/app/coach/clients/page.tsx`** (Server Component):

1. After fetching the player list (existing query), fetch attendance data for all players in a single query:
```typescript
const memberIds = players.map(p => p.id)
const { data: attendanceData } = await supabase
  .from('session_rsvps')
  .select('member_id, created_at, sessions!inner(scheduled_at)')
  .in('member_id', memberIds)
  .eq('rsvp_type', 'confirmed')
  .is('cancelled_at', null)
```
If the `sessions!inner(scheduled_at)` join syntax fails at runtime, fall back to a two-step approach: fetch RSVP records, then fetch sessions by ID, merge in JS. The two-query merge is already an established pattern in this codebase (see coach/page.tsx).

2. Process attendance data in JS: group by `member_id`, extract min `scheduled_at` (first lesson) and max `scheduled_at` (last session) per member. Store as `Map<string, { firstLesson: string | null, lastSession: string | null }>`.

3. Update the page heading to match UI-SPEC: `font-heading font-bold text-2xl text-foreground mb-4` — "Players ({count})" where count is the number of players.

4. Update each player row card to match UI-SPEC layout:
```
bg-card rounded-2xl border border-border/50 p-4 active:scale-[0.98] transition-colors
```
   - Row layout: `flex items-center gap-3`
   - Left: Avatar (existing InitialsAvatar or img) — `w-10 h-10 rounded-xl`
   - Middle (flex-1):
     - Name: `text-sm font-bold text-foreground`
     - If attendance data exists:
       - "Last session: {date}" — `text-[10px] text-muted-foreground` — date formatted as "1 Apr 2026"
       - "First lesson: {date}" — `text-[10px] text-muted-foreground` — date formatted as "12 Jan 2026"
     - If no attendance: "No sessions yet" — `text-[10px] text-muted-foreground italic`
   - Right: Lucide `ChevronRight` icon for navigation affordance

5. **Empty state** (no players):
   - Lucide `Users` icon `w-8 h-8 text-muted mb-3` centered
   - Heading: `font-heading font-bold text-base` — "No players yet"
   - Body: `text-sm text-muted-foreground` — "Players will appear here once they've set up their profiles."

6. **Loading skeleton** (add `loading.tsx` at `src/app/coach/clients/loading.tsx`):
   - 5 rows of `bg-muted animate-pulse rounded-2xl h-[72px]`

7. Update page container padding to match UI-SPEC: `px-5 pt-14 pb-24` (adjust from current top-nav padding to bottom-nav padding — Plan 04 converts nav, but pre-set the bottom padding now).
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/coach/clients/page.tsx contains `session_rsvps` (attendance query)
    - src/app/coach/clients/page.tsx contains `Last session:` (attendance display)
    - src/app/coach/clients/page.tsx contains `First lesson:` (attendance display)
    - src/app/coach/clients/page.tsx contains `No sessions yet` (no attendance state)
    - src/app/coach/clients/page.tsx contains `Players (` or `Players ({` (heading with count)
    - src/app/coach/clients/page.tsx contains `No players yet` (empty state heading)
    - src/app/coach/clients/page.tsx contains `ChevronRight` (row navigation affordance)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Player roster shows last attended date and first lesson date per player, empty state with Users icon, loading skeleton, heading with player count</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coach page -> Session data | Coach can only see sessions in their community (RLS enforced) |
| Client page -> Attendance data | session_rsvps RLS scopes to community |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | coach/page.tsx attendee query | accept | RLS on session_rsvps already scopes to community_id; coach sees all members in their community (expected behavior) |
| T-04-10 | Information Disclosure | coach/clients attendance | accept | Attendance data is community-scoped via RLS; coach seeing player attendance is the intended use case |
</threat_model>

<verification>
- `npx next build` passes
- Coach schedule page renders with day/week toggle
- Player roster shows attendance dates
- localStorage preference persists view choice
</verification>

<success_criteria>
- Coach can toggle day/week views with persistent preference
- Day view shows session cards with attendee avatar strips
- Player roster rows display first lesson and last session dates
- All components match UI-SPEC styling
</success_criteria>

<output>
After completion, create `.planning/phases/04-coach-dashboard-community-events/04-03-SUMMARY.md`
</output>
