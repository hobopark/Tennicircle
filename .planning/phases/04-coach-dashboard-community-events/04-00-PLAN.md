---
phase: 04-coach-dashboard-community-events
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/events.test.ts
  - src/lib/actions/announcements.test.ts
  - src/components/calendar/WeekCalendarGrid.test.tsx
  - src/components/events/EventCard.test.tsx
autonomous: true
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - EVNT-04
  - EVNT-05
  - EVNT-06
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Test stub files exist for all Wave 0 gaps identified in RESEARCH.md"
    - "Each test file has at least one pending/skipped test per requirement it covers"
    - "npx vitest run completes without configuration errors"
  artifacts:
    - path: "src/lib/actions/events.test.ts"
      provides: "Test stubs for event CRUD and RSVP actions"
      contains: "createEvent"
    - path: "src/lib/actions/announcements.test.ts"
      provides: "Test stubs for announcement actions"
      contains: "createAnnouncement"
    - path: "src/components/calendar/WeekCalendarGrid.test.tsx"
      provides: "Test stubs for calendar day/week view"
      contains: "day view"
    - path: "src/components/events/EventCard.test.tsx"
      provides: "Test stubs for event card rendering"
      contains: "EventCard"
  key_links: []
---

<objective>
Create test stub files for all Phase 4 Wave 0 gaps identified in RESEARCH.md.

Purpose: Satisfy Nyquist verification requirement — every plan task must have an automated test command that references real test files. These stubs define the behavioral contracts that implementation tasks will make pass.
Output: 4 test files with pending/skipped tests covering EVNT-01 through EVNT-06, DASH-01, DASH-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-coach-dashboard-community-events/04-CONTEXT.md
@.planning/phases/04-coach-dashboard-community-events/04-RESEARCH.md
@.planning/phases/04-coach-dashboard-community-events/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create event and announcement action test stubs</name>
  <files>src/lib/actions/events.test.ts, src/lib/actions/announcements.test.ts</files>
  <read_first>
    src/lib/actions/rsvps.ts
    src/lib/types/events.ts
    src/lib/validations/events.ts
    vitest.config.ts
  </read_first>
  <action>
**Create `src/lib/actions/events.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest'

// These tests will be filled in as implementation proceeds.
// They define the behavioral contracts for event server actions.

describe('createEvent', () => {
  it.todo('creates a tournament event with valid data (EVNT-01)')
  it.todo('creates a social event with capacity (EVNT-02)')
  it.todo('creates an open session event (EVNT-03)')
  it.todo('computes is_official from JWT role, not form data (EVNT-05)')
  it.todo('returns validation errors for missing required fields')
  it.todo('rejects unauthenticated users')
})

describe('rsvpEvent', () => {
  it.todo('inserts confirmed RSVP when capacity available (EVNT-04)')
  it.todo('inserts waitlisted RSVP when event is full')
  it.todo('prevents duplicate RSVP for same event')
  it.todo('rejects RSVP to cancelled event')
})

describe('cancelEventRsvp', () => {
  it.todo('sets cancelled_at on existing RSVP')
  it.todo('is idempotent for already-cancelled RSVP')
})

describe('deleteEvent', () => {
  it.todo('allows creator to delete their event')
  it.todo('allows admin to delete any event')
  it.todo('rejects deletion by non-creator non-admin')
})
```

**Create `src/lib/actions/announcements.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest'

describe('createAnnouncement', () => {
  it.todo('creates announcement when user is coach (EVNT-05)')
  it.todo('creates announcement when user is admin (EVNT-05)')
  it.todo('rejects announcement creation by client role')
  it.todo('returns validation errors for missing title or body')
})
```

These are `.todo()` stubs — they will show as pending in vitest output, not as failures. Implementation plans will convert them to real tests as server actions are built.
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx vitest run src/lib/actions/events.test.ts src/lib/actions/announcements.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>events.test.ts has 13 todo tests covering EVNT-01 through EVNT-05; announcements.test.ts has 4 todo tests covering EVNT-05. All show as pending (not failing) in vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Create calendar and event card component test stubs</name>
  <files>src/components/calendar/WeekCalendarGrid.test.tsx, src/components/events/EventCard.test.tsx</files>
  <read_first>
    src/components/calendar/WeekCalendarGrid.tsx
    vitest.config.ts
  </read_first>
  <action>
**Create `src/components/calendar/WeekCalendarGrid.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest'

describe('WeekCalendarGrid', () => {
  describe('day view (DASH-01)', () => {
    it.todo('renders day view when view state is "day"')
    it.todo('shows date navigation with previous/next day controls')
    it.todo('filters sessions to selected date only')
    it.todo('shows empty state when no sessions for selected day')
  })

  describe('attendance preview (DASH-02)', () => {
    it.todo('shows confirmed/capacity count on session cards')
    it.todo('shows first 5 attendee avatars in strip')
    it.todo('shows overflow pill when more than 5 attendees')
  })

  describe('view toggle persistence', () => {
    it.todo('defaults to week view on initial render')
    it.todo('persists view choice to localStorage')
  })
})
```

**Create `src/components/events/EventCard.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest'

describe('EventCard', () => {
  it.todo('renders tournament badge with correct styling (EVNT-06)')
  it.todo('renders social badge with correct styling (EVNT-06)')
  it.todo('renders open_session badge with correct styling (EVNT-06)')
  it.todo('shows spots remaining when capacity is set')
  it.todo('shows "Full" pill when rsvp_count >= capacity')
  it.todo('shows RSVP button when user has no RSVP')
  it.todo('shows "Going" badge when user has confirmed RSVP')
  it.todo('shows "Waitlisted" badge when user is waitlisted')
})
```

These stubs establish the rendering contracts. Implementation plans will fill them in once the components exist.
  </action>
  <verify>
    <automated>cd "/Users/hoboparkbench/Documents/Park Bench Labs/Tennicircle" && npx vitest run src/components/calendar/WeekCalendarGrid.test.tsx src/components/events/EventCard.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>WeekCalendarGrid.test.tsx has 9 todo tests covering DASH-01 and DASH-02; EventCard.test.tsx has 8 todo tests covering EVNT-06. All show as pending in vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stubs only — no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | accept | Test files contain no production logic |
</threat_model>

<verification>
- `npx vitest run` completes without configuration errors
- All 4 test files exist and are detected by vitest
- All tests show as "todo/pending" (not "fail")
</verification>

<success_criteria>
- 4 test stub files created matching RESEARCH.md Wave 0 gaps
- vitest runs successfully with all tests in pending state
- Subsequent plans can reference these test files in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/04-coach-dashboard-community-events/04-00-SUMMARY.md`
</output>
