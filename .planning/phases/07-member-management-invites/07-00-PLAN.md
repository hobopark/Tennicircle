---
phase: 07-member-management-invites
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/actions/invites.test.ts
  - src/lib/actions/members.test.ts
  - src/components/members/MemberCard.test.tsx
autonomous: true
requirements: [MGMT-01, MGMT-02, MGMT-03, MGMT-04, MGMT-05, MGMT-06, MGMT-07]
user_setup: []

must_haves:
  truths:
    - "Test stub files exist for all Phase 7 requirement behaviors"
    - "All stubs use it.todo and pass vitest run without failures"
  artifacts:
    - path: "src/lib/actions/invites.test.ts"
      provides: "Test stubs for MGMT-01 invite behaviors"
      contains: "describe"
    - path: "src/lib/actions/members.test.ts"
      provides: "Test stubs for MGMT-02, MGMT-03, MGMT-04, MGMT-06 member action behaviors"
      contains: "describe"
    - path: "src/components/members/MemberCard.test.tsx"
      provides: "Test stubs for MGMT-05, MGMT-07 component behaviors"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 7 requirements so that subsequent plans can include vitest in their verify blocks.

Purpose: VALIDATION.md identifies 3 test files as Wave 0 gaps. These stubs use `it.todo` to define expected behaviors without implementation, following the established pattern from existing test files (e.g., `announcements.test.ts`). They must exist before Plans 01-03 execute so vitest commands in verify blocks do not fail on missing files.

Output: 3 test stub files with it.todo entries covering all MGMT requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-member-management-invites/07-CONTEXT.md
@.planning/phases/07-member-management-invites/07-VALIDATION.md

<interfaces>
From src/lib/actions/announcements.test.ts (established test stub pattern):
```typescript
import { describe, it } from 'vitest'

describe('createAnnouncement', () => {
  it.todo('creates announcement when user is coach (EVNT-05)')
  it.todo('creates announcement when user is admin (EVNT-05)')
  it.todo('rejects announcement creation by client role')
  it.todo('returns validation errors for missing title or body')
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for all Phase 7 requirements</name>
  <files>src/lib/actions/invites.test.ts, src/lib/actions/members.test.ts, src/components/members/MemberCard.test.tsx</files>
  <read_first>
    - src/lib/actions/announcements.test.ts (established test stub pattern to follow exactly)
    - vitest.config.mts (confirm jsdom environment and globals)
  </read_first>
  <action>
Create three test stub files following the exact pattern from `announcements.test.ts`.

**1. src/lib/actions/invites.test.ts** (covers MGMT-01):

```typescript
import { describe, it } from 'vitest'

describe('createInviteLink', () => {
  it.todo('creates client invite link when user is coach (MGMT-01)')
  it.todo('creates coach invite link when user is admin (MGMT-01)')
  it.todo('rejects coach invite creation by non-admin coach (MGMT-01)')
  it.todo('rejects invite creation by client role')
  it.todo('returns error when user is not authenticated')
})

describe('revokeInviteLink', () => {
  it.todo('revokes an active invite link')
  it.todo('rejects revocation by non-creator')
})
```

**2. src/lib/actions/members.test.ts** (covers MGMT-02, MGMT-03, MGMT-04, MGMT-06):

```typescript
import { describe, it } from 'vitest'

describe('updateMemberRole', () => {
  it.todo('promotes client to coach when caller is admin (MGMT-02)')
  it.todo('grants admin privilege to any member when caller is admin (MGMT-03)')
  it.todo('rejects role change by non-admin caller (MGMT-02)')
  it.todo('returns error for non-existent member ID')
})

describe('removeMember', () => {
  it.todo('removes member when caller is admin (MGMT-02)')
  it.todo('rejects member removal by non-admin caller (MGMT-02)')
  it.todo('returns error for non-existent member ID')
})

describe('joinCommunityAsClient', () => {
  it.todo('creates client membership in single community for pending user (MGMT-04)')
  it.todo('returns error when user is already a community member (MGMT-04)')
  it.todo('returns error when no community exists (MGMT-04)')
  it.todo('returns error when user is not authenticated')
})

describe('assignClient', () => {
  it.todo('assigns client to coach when caller is coach (MGMT-06)')
  it.todo('assigns client to coach when caller is admin (MGMT-06)')
  it.todo('rejects assignment by client role (MGMT-06)')
  it.todo('returns error for duplicate assignment (MGMT-06)')
})

describe('removeClientAssignment', () => {
  it.todo('removes client assignment when caller is coach (MGMT-06)')
  it.todo('rejects removal by client role (MGMT-06)')
})

describe('processInviteSignup', () => {
  it.todo('creates community membership from valid invite token')
  it.todo('inserts coach_client_assignments row for client invite (D-10)')
  it.todo('does not set deprecated coach_id column (D-10)')
  it.todo('returns error for invalid invite token')
  it.todo('returns error for revoked invite')
})
```

**3. src/components/members/MemberCard.test.tsx** (covers MGMT-05, MGMT-07):

```typescript
import { describe, it } from 'vitest'

describe('MemberCard', () => {
  it.todo('renders display name and role badge (MGMT-05)')
  it.todo('renders "Profile pending" badge when hasProfile is false (MGMT-07)')
  it.todo('renders dimmed card (opacity-60) when hasProfile is false (MGMT-07)')
  it.todo('renders assigned coach names for client members (D-06)')
  it.todo('renders last session date when available (D-06)')
  it.todo('shows expand button for coach viewer on non-self cards')
  it.todo('shows "Assign to me" button for unassigned clients when viewer is coach (D-09)')
  it.todo('shows "Remove from my clients" button for assigned clients when viewer is coach (D-09)')
  it.todo('shows admin role actions (Promote, Grant Admin, Remove) when viewer is admin (D-07)')
  it.todo('hides all actions on self card')
})

describe('RosterToggle', () => {
  it.todo('renders My clients and All members options (MGMT-05)')
  it.todo('highlights selected option')
  it.todo('calls onChange with correct value on click')
})
```

Note: The `src/components/members/` directory may not exist yet at Wave 0. Create the directory as part of this task. The MemberCard component itself will be created in Plan 03 -- these are just stub tests that define expected behavior.
  </action>
  <verify>
    <automated>test -f src/lib/actions/invites.test.ts && test -f src/lib/actions/members.test.ts && test -f src/components/members/MemberCard.test.tsx && npx vitest run src/lib/actions/invites.test.ts src/lib/actions/members.test.ts src/components/members/MemberCard.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 test stub files exist with it.todo entries covering every MGMT requirement; vitest run passes (all todos are skipped, no failures)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stub files only — no production code or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No security-relevant code in test stubs |
</threat_model>

<verification>
- All 3 test files exist
- npx vitest run completes without errors (todos are skipped, not failed)
- Test descriptions map to MGMT requirement IDs
</verification>

<success_criteria>
- src/lib/actions/invites.test.ts exists with MGMT-01 stubs
- src/lib/actions/members.test.ts exists with MGMT-02, MGMT-03, MGMT-04, MGMT-06 stubs
- src/components/members/MemberCard.test.tsx exists with MGMT-05, MGMT-07 stubs
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-member-management-invites/07-00-SUMMARY.md`
</output>
