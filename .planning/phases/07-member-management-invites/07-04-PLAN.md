---
phase: 07-member-management-invites
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified: []
autonomous: false
requirements: [MGMT-01, MGMT-02, MGMT-03, MGMT-04, MGMT-05, MGMT-06, MGMT-07]
user_setup:
  - service: supabase
    why: "Junction table migration must be applied to live database"
    dashboard_config:
      - task: "Run migration SQL in Supabase SQL Editor"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "coach_client_assignments table exists in live Supabase database"
    - "RLS policies on coach_client_assignments are active"
    - "Roster page renders without errors"
    - "All Phase 7 requirements pass verification"
  artifacts: []
  key_links: []
---

<objective>
Apply the database migration via Supabase SQL Editor (user manual step) and verify the entire Phase 7 implementation end-to-end.

Purpose: The schema changes from Plan 01 must be applied to the live database before the roster page and assignment actions can function. Per project preference, Joon pushes migrations manually via SQL Editor. This plan gates that manual step and then runs full verification.

Output: Live database with junction table, verified working roster page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-member-management-invites/07-CONTEXT.md
@.planning/phases/07-member-management-invites/07-01-SUMMARY.md
@.planning/phases/07-member-management-invites/07-02-SUMMARY.md
@.planning/phases/07-member-management-invites/07-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply junction table migration via Supabase SQL Editor</name>
  <files>supabase/migrations/00008_coach_client_assignments.sql</files>
  <read_first>
    - supabase/migrations/00008_coach_client_assignments.sql (the migration SQL to apply)
  </read_first>
  <action>
Present the contents of `supabase/migrations/00008_coach_client_assignments.sql` to the user and instruct them to:

1. Open the Supabase Dashboard
2. Navigate to SQL Editor
3. Paste the full migration SQL
4. Execute it
5. Confirm the table and policies were created

The SQL creates:
- `coach_client_assignments` table with `id`, `community_id`, `coach_member_id`, `client_member_id`, `assigned_at`
- Unique constraint on `(coach_member_id, client_member_id)`
- RLS enabled with 3 policies: read (community-scoped), insert (coach can insert own), delete (coach can delete own, admin can delete any)
- DEPRECATED comment on `community_members.coach_id`

After user confirms, proceed to Task 2 for functional verification.
  </action>
  <verify>
    <automated>echo "Manual step — user confirms migration applied in Supabase SQL Editor"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms migration was executed successfully in Supabase SQL Editor
    - No SQL errors reported
  </acceptance_criteria>
  <done>Junction table migration applied to live Supabase database</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of member management features</name>
  <files>src/app/coach/clients/page.tsx</files>
  <action>
Run the dev server and verify all Phase 7 features end-to-end. Present the verification checklist below to the user.

Pre-check: Start dev server with `npm run dev` if not running. Run `npx tsc --noEmit` to confirm no type errors. Run `npx vitest run` to confirm tests pass.

Verification scenarios:

1. Roster page renders (MGMT-05, D-04): Log in as coach, navigate to /coach/clients, verify "Members (N)" title and toggle visible.

2. Profile pending members (MGMT-07, D-05): Verify members without profiles appear with dimmed style and "Profile pending" badge.

3. Member cards (D-06): Verify cards show display name, role badge, assigned coach name, last session date.

4. Invite link (MGMT-01, D-01, D-02, D-03): As coach click Invite, verify clipboard copy + toast. As admin verify role selector appears.

5. Admin role management (MGMT-02, MGMT-03, D-07, D-08): As admin expand a member card, verify Promote to Coach, Grant Admin, Remove actions. Verify Remove uses styled Dialog with orange button.

6. Coach client assignment (MGMT-06, D-09): As coach in All members view, assign a client, verify it appears in My clients. Then unassign.

7. Open sign-up (MGMT-04): Sign up without invite link, verify auto-join as client on /welcome page.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | tail -3 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Roster page renders at /coach/clients without errors
    - Toggle switches between My clients and All members views
    - Profile-pending members are visible (not hidden)
    - Invite link copies to clipboard with toast
    - Admin sees role selector on invite; coach does not
    - Admin role management actions appear on expanded member cards
    - Remove member shows styled Dialog with orange button
    - Coach can assign and unassign clients
    - Open sign-up user auto-joins as client
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>All Phase 7 MGMT requirements verified working end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is verification-only, no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | Migration SQL in SQL Editor | accept | Migration is applied by project owner (Joon) in their own Supabase dashboard — no credential exposure |
</threat_model>

<verification>
- Migration applied to live database
- All 7 MGMT requirements verified via manual testing
- No console errors on roster page
- TypeScript compiles cleanly: `npx tsc --noEmit`
- Vitest passes: `npx vitest run`
</verification>

<success_criteria>
- Junction table exists in live Supabase database
- User confirms all 7 verification scenarios pass
- Phase 7 is complete and ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/07-member-management-invites/07-04-SUMMARY.md`
</output>
