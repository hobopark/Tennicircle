---
phase: 08-community-selector
plan: 05
type: execute
wave: 5
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - src/components/communities/PendingRequestsSection.tsx
  - src/components/communities/RejectRequestDialog.tsx
  - src/app/c/[slug]/coach/clients/page.tsx
  - src/app/c/[slug]/coach/clients/RosterClientWrapper.tsx
  - src/components/nav/AppNav.tsx
  - src/app/c/[slug]/sessions/calendar/loading.tsx (NEW — not moved from old path)
  - src/app/c/[slug]/notifications/loading.tsx (NEW — not moved from old path)
  - src/app/c/[slug]/admin/loading.tsx (NEW — not moved from old path)
autonomous: false
requirements: [COMM-04, COMM-06]
user_setup:
  - service: supabase
    why: "Schema migration must be pushed manually via SQL Editor"
    dashboard_config:
      - task: "Execute migration SQL in Supabase SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> New query"
      - task: "Remove Custom Access Token Hook binding"
        location: "Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token"
must_haves:
  truths:
    - "Coaches and admins see pending join requests at top of roster page"
    - "Approve creates community_members row and sends notification"
    - "Reject updates status and sends notification with orange confirm button (not red)"
    - "Clients tab shows pending count badge"
    - "Every /c/[slug]/ route has a loading.tsx skeleton"
    - "Schema migration is pushed to Supabase"
  artifacts:
    - path: "src/components/communities/PendingRequestsSection.tsx"
      provides: "Pending requests list with approve/reject buttons"
      contains: "approveJoinRequest"
    - path: "src/components/communities/RejectRequestDialog.tsx"
      provides: "Orange-themed reject confirmation dialog"
      contains: "bg-orange-500"
    - path: "src/app/c/[slug]/coach/loading.tsx"
      provides: "Coach dashboard loading skeleton"
      contains: "Skeleton"
  key_links:
    - from: "src/components/communities/PendingRequestsSection.tsx"
      to: "src/lib/actions/communities.ts"
      via: "approveJoinRequest + rejectJoinRequest"
      pattern: "approveJoinRequest|rejectJoinRequest"
    - from: "src/components/nav/AppNav.tsx"
      to: "src/lib/actions/communities.ts"
      via: "getPendingRequests for badge count"
      pattern: "getPendingRequests"
---

<objective>
Complete the join request approval UI (pending requests on roster, approve/reject with notifications), add loading skeletons to all community-scoped routes, push the schema migration to Supabase, and remove the Custom Access Token Hook.

Purpose: This plan completes the last user-facing features (approval flow + loading states) and performs the critical database migration that enables everything.
Output: Full join request lifecycle working end-to-end. Schema live in Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-community-selector/08-CONTEXT.md
@.planning/phases/08-community-selector/08-UI-SPEC.md
@.planning/phases/08-community-selector/08-01-SUMMARY.md
@.planning/phases/08-community-selector/08-03-SUMMARY.md
@.planning/phases/08-community-selector/08-04-SUMMARY.md

<interfaces>
From src/lib/actions/communities.ts (Plan 01):
```typescript
export async function approveJoinRequest(requestId: string, communitySlug: string): Promise<{ success; error? }>
export async function rejectJoinRequest(requestId: string, communitySlug: string): Promise<{ success; error? }>
export async function getPendingRequests(communityId: string): Promise<{ success; data?: PendingRequest[] }>
```

From src/lib/context/community.tsx (Plan 01):
```typescript
export function useCommunity(): { communityId; communitySlug; communityName; membershipId; role }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pending requests section, reject dialog, roster integration, nav badge, and loading skeletons</name>
  <files>
    src/components/communities/PendingRequestsSection.tsx
    src/components/communities/RejectRequestDialog.tsx
    src/app/c/[slug]/coach/clients/page.tsx
    src/app/c/[slug]/coach/clients/RosterClientWrapper.tsx
    src/components/nav/AppNav.tsx
    src/app/c/[slug]/coach/loading.tsx
    src/app/c/[slug]/coach/schedule/loading.tsx
    src/app/c/[slug]/coach/clients/loading.tsx
    src/app/c/[slug]/sessions/loading.tsx
    src/app/c/[slug]/sessions/calendar/loading.tsx
    src/app/c/[slug]/events/loading.tsx
    src/app/c/[slug]/notifications/loading.tsx
    src/app/c/[slug]/admin/loading.tsx
  </files>
  <read_first>
    src/app/c/[slug]/coach/clients/page.tsx
    src/app/c/[slug]/coach/clients/RosterClientWrapper.tsx
    src/components/nav/AppNav.tsx
    src/components/ui/dialog.tsx
    src/components/profile/InitialsAvatar.tsx
    .planning/phases/08-community-selector/08-UI-SPEC.md
    .planning/phases/08-community-selector/08-CONTEXT.md
  </read_first>
  <action>
**A. Create `src/components/communities/RejectRequestDialog.tsx`** ('use client'):

Per UI-SPEC "Reject Confirmation Dialog" and D-40:
- Props: `open: boolean, onOpenChange: (open: boolean) => void, memberName: string, requestId: string, communitySlug: string`
- Uses shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`
- Title: "Reject request?"
- Body: `Remove {memberName}'s request to join this community. They will be notified.`
- Cancel button: `Button variant="outline"` — "Cancel" — closes dialog
- Confirm button: `className="bg-orange-500 hover:bg-orange-600 text-white"` — "Reject Request"
  - On click: `useTransition` loading state, call `rejectJoinRequest(requestId, communitySlug)`
  - On success: close dialog, toast "Request rejected." (neutral)
  - On error: toast with error message

**B. Create `src/components/communities/PendingRequestsSection.tsx`** ('use client'):

Per UI-SPEC "Pending Requests Section (Roster Page)" and D-37, D-38:
- Props: `requests: Array<{ id, userName, userAvatarUrl?, userId, createdAt }>`, `communitySlug: string`
- Wrapper: `mb-6 rounded-2xl border border-orange-200 bg-orange-50 dark:bg-orange-950/20 dark:border-orange-900/50 p-4`
- Header: "Pending Requests" (`font-heading font-bold text-base`) + count badge (`bg-orange-100 text-orange-700 text-xs font-bold px-2 py-0.5 rounded-full ml-2`)
- Per request row: flex layout with:
  - `InitialsAvatar` component with name/avatar
  - Member name (`text-sm font-medium`) + join date (`text-xs text-muted-foreground`) formatted with `Intl.DateTimeFormat('en-AU', { timeZone: 'Australia/Sydney' })`
  - "Approve Member" button: primary variant, compact (`text-xs h-8`). On click: `useTransition`, call `approveJoinRequest(request.id, communitySlug)`. On success: toast "Approved. {name} is now a member." — row removed optimistically.
  - "Reject" button: `variant="outline"` with orange hover state (`hover:border-orange-400 hover:text-orange-600`), compact. On click: open `RejectRequestDialog`

**C. Update `src/app/c/[slug]/coach/clients/page.tsx`:**

Add pending requests fetch at the top of the page:
1. Read the current page content
2. Add import for `getPendingRequests` from `@/lib/actions/communities`
3. In the server component, after getting community data, fetch pending requests:
   ```typescript
   const { data: pendingRequests } = await getPendingRequests(community.id)
   ```
4. Pass `pendingRequests` and `communitySlug` (from params) to the client wrapper
5. The client wrapper renders `PendingRequestsSection` at the TOP of the page, above the existing roster content, only when `role === 'admin' || role === 'coach'` (D-38)

**D. Update `src/app/c/[slug]/coach/clients/RosterClientWrapper.tsx`:**
- Add props for `pendingRequests` and `communitySlug`
- Render `PendingRequestsSection` when `pendingRequests.length > 0` and user role is admin or coach
- Get role from `useCommunity()` context

**E. Update `src/components/nav/AppNav.tsx` — Clients tab badge (D-41):**

Add a pending request count badge to the Clients tab:
1. The AppNav needs to know the pending count. Two approaches:
   - Pass it as a prop from the community layout (requires layout change)
   - Fetch it client-side in AppNav on mount
   
   Use client-side fetch approach to avoid modifying the layout. **Note: AppNav already uses `useMaybeCommunity()` (from Plan 03). Guard the fetch with a null check:**
   ```typescript
   const community = useMaybeCommunity()
   const [pendingCount, setPendingCount] = useState(0)
   useEffect(() => {
     if (community && (community.role === 'admin' || community.role === 'coach')) {
       getPendingRequests(community.communityId).then(result => {
         if (result.success && result.data) setPendingCount(result.data.length)
       })
     }
   }, [community?.communityId, community?.role])
   ```
2. On the Clients tab icon/label, render a count badge when `pendingCount > 0`:
   ```
   bg-orange-500 text-white text-[10px] font-bold min-w-[16px] h-4 rounded-full flex items-center justify-center absolute -top-1 -right-1
   ```
3. Badge: `aria-label="{pendingCount} pending join request{pendingCount !== 1 ? 's' : ''}"`

**F. Create MISSING loading.tsx skeleton files for routes that don't already have them (D-49):**

**IMPORTANT:** Plan 03 Task 2 already moved existing loading.tsx files from old paths to /c/[slug]/. The following loading.tsx files ALREADY EXIST after Plan 03 (do NOT recreate):
- `src/app/c/[slug]/coach/loading.tsx` (moved from coach/loading.tsx)
- `src/app/c/[slug]/coach/schedule/loading.tsx` (moved from coach/schedule/loading.tsx)
- `src/app/c/[slug]/coach/clients/loading.tsx` (moved from coach/clients/loading.tsx)
- `src/app/c/[slug]/sessions/loading.tsx` (moved from sessions/loading.tsx)
- `src/app/c/[slug]/events/loading.tsx` (moved from events/loading.tsx)

**Create ONLY the loading.tsx files that DON'T yet exist:**

Per UI-SPEC "Loading Skeletons" section. Each uses the `Skeleton` component:

```tsx
import { Skeleton } from '@/components/ui/skeleton'

export default function Loading() {
  return (
    <div className="px-5 pt-6 pb-24 flex flex-col gap-4">
      <Skeleton className="h-8 w-48 rounded-lg" />
      <Skeleton className="h-20 rounded-2xl" />
      <Skeleton className="h-20 rounded-2xl" />
      <Skeleton className="h-20 rounded-2xl" />
    </div>
  )
}
```

Create these NEW files (only if they don't already exist after Plan 03):
- `src/app/c/[slug]/sessions/calendar/loading.tsx` — header + calendar grid placeholder
- `src/app/c/[slug]/notifications/loading.tsx` — header + 4 notification rows
- `src/app/c/[slug]/admin/loading.tsx` — header + 2 admin panels

**Verify step:** After creating, run `find src/app/c/\[slug\] -name "loading.tsx" | wc -l` — should be at least 8 (5 moved + 3 new + any others from nested routes). If any of the "already exist" files are somehow missing, create them too.
  </action>
  <verify>
    <automated>cd /Users/hoboparkbench/Documents/Park\ Bench\ Labs/Tennicircle && ls src/app/c/\[slug\]/*/loading.tsx src/app/c/\[slug\]/*/*/loading.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/communities/PendingRequestsSection.tsx` exists and contains `approveJoinRequest`
    - `src/components/communities/PendingRequestsSection.tsx` contains `InitialsAvatar`
    - `src/components/communities/PendingRequestsSection.tsx` contains `bg-orange-100` (badge)
    - `src/components/communities/RejectRequestDialog.tsx` exists and contains `bg-orange-500`
    - `src/components/communities/RejectRequestDialog.tsx` contains `Reject Request` button text
    - `src/components/communities/RejectRequestDialog.tsx` does NOT contain `bg-destructive` or `bg-red` (no red per project memory)
    - `src/app/c/[slug]/coach/clients/page.tsx` contains `getPendingRequests`
    - `src/components/nav/AppNav.tsx` contains pending count badge logic
    - At least 8 `loading.tsx` files exist under `src/app/c/[slug]/`
    - Every `loading.tsx` contains `Skeleton` import
    - `npx vitest run` passes
  </acceptance_criteria>
  <done>Pending requests section on roster with approve/reject, reject dialog with orange theme, Clients tab badge, and 8 loading skeletons across all routes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push schema migration to Supabase and remove Custom Access Token Hook</name>
  <files>supabase/migrations/00009_phase8_community_selector.sql</files>
  <action>
    Human must perform 3 manual steps in Supabase Dashboard that cannot be automated:
    1. Push the SQL migration via SQL Editor
    2. Remove the Custom Access Token Hook binding
    3. Add SUPABASE_SERVICE_ROLE_KEY to .env.local and Vercel env vars
    Detailed instructions are in the how-to-verify section below.
  </action>
  <what-built>
    Complete Phase 8 implementation: RLS policy rewrite, route migration to /c/[slug]/, community picker page, join request flow, loading skeletons. The SQL migration must be pushed to make everything work.
  </what-built>
  <how-to-verify>
    **Step 1: Push the SQL migration**
    1. Open Supabase Dashboard -> SQL Editor -> New query
    2. Copy the ENTIRE contents of `supabase/migrations/00009_phase8_community_selector.sql`
    3. Paste into the SQL Editor and click "Run"
    4. Verify: no errors in output. All policies should be created successfully.

    **Step 2: Remove Custom Access Token Hook (D-10)**
    1. Go to Supabase Dashboard -> Authentication -> Hooks
    2. Find "Custom Access Token" hook
    3. Click to disable/remove it
    4. The JWT will now be vanilla (no community_id or user_role claims)

    **Step 3: Add SUPABASE_SERVICE_ROLE_KEY to environment**
    1. Go to Supabase Dashboard -> Settings -> API
    2. Copy the "service_role" key (secret)
    3. Add to `.env.local`: `SUPABASE_SERVICE_ROLE_KEY=your_key_here`
    4. If deployed on Vercel, add this env var there too

    **Step 4: Verify the app works**
    1. Run `npm run dev`
    2. Log in — you should be redirected to /communities
    3. Your community should appear in "Your Communities"
    4. Click "Go to Community" — should navigate to /c/{slug}/{role-home}
    5. All tabs in AppNav should work with the new /c/{slug}/ paths
    6. Check that coach dashboard, sessions, events, notifications all load correctly
  </how-to-verify>
  <verify>Human confirms migration ran successfully and app navigation works with new route structure</verify>
  <done>Schema migration pushed. Custom Access Token Hook removed. Service role key configured. App verified working with community-scoped routes.</done>
  <resume-signal>Type "approved" if migration ran successfully and app works, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approve/Reject -> DB | Only admin/coach can resolve requests; server action validates |
| Notification insert | Uses service_role client; never exposes service key to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Elevation of Privilege | approveJoinRequest | mitigate | Server action queries community_members to verify caller is admin/coach before updating. RLS also enforces. |
| T-08-17 | Information Disclosure | getPendingRequests | mitigate | Returns only name and join date, not email or sensitive profile data. Only admin/coach can call. |
| T-08-18 | Spoofing | SUPABASE_SERVICE_ROLE_KEY | mitigate | Key stored in .env.local (git-ignored) and Vercel env vars. Never exposed to browser. Only used server-side for notification inserts. |
| T-08-19 | Tampering | Schema migration | mitigate | Migration is idempotent (DROP IF EXISTS + CREATE). Can be re-run safely. |
</threat_model>

<verification>
- Loading skeletons count: `ls src/app/c/[slug]/*/loading.tsx src/app/c/[slug]/*/*/loading.tsx 2>/dev/null | wc -l` returns at least 8
- No red/destructive colors in reject dialog: `grep -c "bg-red\|bg-destructive" src/components/communities/RejectRequestDialog.tsx` returns 0
- Test suite: `npx vitest run`
- Manual: after schema push, full app navigation works
</verification>

<success_criteria>
- Join request approval flow works end-to-end (request -> approve/reject -> notification)
- Reject dialog uses orange theme (not red)
- Clients tab shows pending count badge
- All 8+ loading skeletons exist
- Schema migration pushed to Supabase
- Custom Access Token Hook removed
- App navigates correctly with new route structure
</success_criteria>

<output>
After completion, create `.planning/phases/08-community-selector/08-05-SUMMARY.md`
</output>
