import { describe, it } from 'vitest'

describe('EventCard', () => {
  it.todo('renders tournament badge with correct styling (EVNT-06)')
  it.todo('renders social badge with correct styling (EVNT-06)')
  it.todo('renders open_session badge with correct styling (EVNT-06)')
  it.todo('shows spots remaining when capacity is set')
  it.todo('shows "Full" pill when rsvp_count >= capacity')
  it.todo('shows RSVP button when user has no RSVP')
  it.todo('shows "Going" badge when user has confirmed RSVP')
  it.todo('shows "Waitlisted" badge when user is waitlisted')
})
